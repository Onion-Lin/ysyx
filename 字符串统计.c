#include <stdio.h>
#include <string.h>

int main(){
    int str[1000];
    int a=0;
    if (getchar()!=stop){
        fgets(*str[];strlen(str[]);stdin);
        a++;
    }
    int i,j;
    int max=(int)strlen(str[0]);
    for (i=0;i<strlen(str[]);i++){
        if ((int)strlen(str[i])>max){
            max=(int)strlen(str[i]);
            j=i;
        }
    }
    for (i=0;i<strlen(str[]);i++){ 
    printf("%d %c",(int)strlen(str[i]),str[j]);
    }
}
